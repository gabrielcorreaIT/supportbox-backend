# Porta em que o servidor HTTP vai escutar.
PORT=4000

# Origem do frontend autorizada a chamar a API (CORS).
ORIGEM_FRONTEND=http://localhost:3000

# Credenciais do Supabase. Serao usadas na etapa do DAO (persistencia).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
