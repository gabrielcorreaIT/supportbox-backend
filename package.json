{
  "name": "supportbox-backend",
  "version": "0.1.0",
  "private": true,
  "description": "API REST do SupportBox - camada Controller e camada Model (dividida em Service e DAO).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
